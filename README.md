# Genom Script VPN PREMIUM UBUNTU 24 


1. install script


```
cd root
rm install
apt install -y && apt update -y && apt upgrade -y && wget -q && chmod +x install && ./install
```

2. UNINSTALL SCRIPT

```
apt install -y && apt update -y && apt upgrade -y && wget -q  && chmod +x uninstall && ./uninstall
```
